"""
HospitalityOS feature modules.

One subpackage per module. `receptionist` is the only one that exists today;
others get a folder here only when work on them actually starts.

Each module owns its own api/, models/, services/ and is wired into the single
FastAPI app in app/main.py.
"""
