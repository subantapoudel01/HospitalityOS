fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
redis==5.2.0
pydantic-settings==2.6.1
tzdata==2026.2            # IANA zones; not present on Windows or slim images

# RAG / knowledge base (Slice B)
pgvector==0.5.0
fastembed==0.8.0          # local ONNX embeddings, no API key needed
google-genai==1.46.0      # only used when AI_*_PROVIDER=gemini
groq==0.34.0              # only used when AI_*_PROVIDER=groq

# auth (staff accounts)
# bcrypt directly rather than passlib[bcrypt]: passlib 1.7.4 (2020) reads
# bcrypt.__about__, removed in bcrypt 4.1, and logs a traceback on every
# hash. Same $2b$ output either way. See app/core/security.py.
bcrypt==4.2.1
PyJWT==2.10.1
email-validator==2.2.0    # pydantic EmailStr

# tests
pytest==9.1.1
pytest-asyncio==1.3.0
httpx==0.28.1
