"""Domain-agnostic retrieval-grounded conversation engine.

Empty at this commit. Step 1 of the extraction creates the package and
nothing else: no module has moved yet, and the application still imports
everything from app.* exactly as before.
"""

__version__ = "0.1.0"
